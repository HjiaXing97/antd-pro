const DEFAULT_TOO_BAR = {
  showCreate: true,
  showExport: true,
  showImport: true,
  hideControls: false,
};

const DEFAULT_SEARCH_FORM = {
  searchText: "查询",
  resetText: "重置",
};
export enum VALUE_TYPE_ENUM {
  SELECT = "select",
  DATE = "date",
  DATE_RANGE = "dateRange",
  TEXTAREA = "textarea",
  PASSWORD = "password",
  RADIO = "radio",
  CHECKBOX = "checkbox",
  UPLOAD = "upload",
  MONEY = "money",
  DICT = "dict",
}

export { DEFAULT_TOO_BAR, DEFAULT_SEARCH_FORM };
